/**
 * dsh-client-paste-image — host half is empty: the upload route belongs to
 * dsh-drop-caret (/api/dsh-drop, content-addressed files under the session
 * cwd's .dsh-drop directory). This package is browser-only; the loader
 * requires a host entry, so this file exists and does nothing.
 */

export function apply() {}

export const inject = [];
